import React from 'react'
import Camera from '../../components/Camera/Camera'

const CreatePage = () => {
  return (
    <section>
      CREATE PAGE
      <Camera />
    </section>
  )
}

export default CreatePage
